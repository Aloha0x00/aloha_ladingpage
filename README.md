# Aloha0x00 Landing Page

Static landing page for Aloha0x00 iOS apps, with Diary with lock as the spotlight product.

## Deploy

This site is ready for Vercel as a static project:

```bash
npx vercel --prod
```
